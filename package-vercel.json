{
  "name": "palostranka",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install-client": "cd client && npm install",
    "build": "cd client && npm install && npm run build",
    "dev": "cd client && npm run dev"
  },
  "description": "Palostranka - Frontend deployed on Vercel, Backend on Railway",
  "license": "MIT"
}
